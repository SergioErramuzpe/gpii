<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the "MyLocation" functionality.
    -->
  

    <application
        android:name=".DBApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ModifyRemovePaisDestinoActivity"></activity>
        <activity android:name=".ModifyRemovePaisOrigenActivity" />
        <activity android:name=".NewPaisOrigenActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".NewRelacionActivity" />
        <activity android:name=".NewPaisDestinoActivity" />
        <activity
            android:name=".AddPaisDestinoActivity"
            android:label="@string/title_activity_add_pais_destino"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".AddRelacionActivity"
            android:label="@string/title_activity_add_relacion"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".AdminActivity" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyCeHrh1DPy95rV2DqctvkE45T0t0kap8fQ" />

        <activity
            android:name=".MapPaisActivity"
            android:label="@string/title_activity_map_pais" />
        <activity
            android:name=".RelacionScrollActivity"
            android:label="@string/title_activity_prueba"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".PaisDestinoActivity"
            android:label="Relaciones Internacionales"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
        </activity>
    </application>

</manifest>